Create today's Chinese AI-curated technology trend report for this repository.

1. Use the `REPORT_DATE`, `REPORT_WINDOW_END`, and `GITHUB_TRENDING_ARG` environment variables prepared by the workflow. Treat the 24 hours ending at 07:00 China Standard Time on `REPORT_DATE` as the editorial focus, not a strict exclusion boundary. Run `python outputs/tech_trend_report.py --window-end "$REPORT_WINDOW_END" $GITHUB_TRENDING_ARG --output outputs/tech-trends-raw.html --candidates-output outputs/tech-trend-candidates.json`. The raw preview is evidence only and must never be published as the final report.
   The trusted Codex configuration includes the `orz_fetch` remote MCP at `https://orz-mcp.netlify.app/mcp`. Use its fetch/read tools for public source pages, repository READMEs, and documentation before using shell networking. Do not use ad-hoc `curl` loops or raw GitHub content URLs when the MCP can fetch the page; if a fetch fails, record the source as unavailable and continue.
2. Read `outputs/tech-trend-candidates.json`. It is evidence, not a final report. Use the first 10 repositories from both current GitHub Trending Daily views: all languages and Chinese spoken language. Deduplicate the two lists and select projects for technical substance, not rank alone. Hacker News and GitHub Trending expose current lists rather than historical snapshots, so they may be used as fresh supplementary signals when a report is generated late or backfilled. Do not claim that these current-list items were historically ranked at the report window end. Prefer RSS and other sources whose publication timestamps fall inside the focus window, but allow recent high-value items outside it.
3. Read every existing report for the previous seven report dates under `outputs/tech-trends-YYYY-MM-DD.html`. Do not repeat an item whose normalized primary URL, GitHub owner/repository, or underlying topic already appeared, unless there is a substantive new event. For any repeat, explain what materially changed and link the new primary evidence.
4. Select exactly 10 substantive items whenever 10 readable, technically meaningful candidates remain after deduplication. On a current-day report with GitHub Trending candidates, include at least 4 distinct `project_trending` repositories in the 10 items. Reserve these slots before filling the remainder with official releases or discussions. Only include fewer than 4 when fewer than 4 technically meaningful Trending repositories remain readable after the 7-day deduplication; state that shortfall and its reason explicitly. Include no more than 2 final-report items from the same organization or publisher. Read each selected primary URL before writing; omit inaccessible sources rather than guessing. Prioritize technical substance, official releases, independently corroborated discussion, GitHub Trending attention, and engineering impact. When fewer than 10 candidates can be verified, state the shortfall explicitly.
   Reserve at least 2 of the 10 slots for the mandatory "值得继续观察" section before filling "今日重点". Select readable, technically meaningful early-stage projects, preliminary results, or developments whose adoption or impact still needs follow-up. These items count toward the total of 10 and may also count toward the GitHub Trending quota. Continue through the remaining candidates to fill these slots; do not place all 10 items in "今日重点". Only fall short when fewer than 2 suitable items can be verified after deduplication; retain the section and explain the shortfall without inventing evidence.
   When using `curl` to verify a source, rely on the workflow-provided command and do not add a custom cache. A failed source must be skipped after its built-in retries; do not repeatedly retry it or block the report on it.
5. Write `outputs/tech-trends-REPORT_DATE.html` using the exact `REPORT_DATE` value. Use `Tech Morning Brief` as the English page title. Describe the preceding 24 hours through 07:00 China Standard Time as the primary observation window, while clearly labeling any current-list or recent supplementary signals outside that window. Include a Chinese editorial overview and, for each item: what happened, core technical point, why it matters, evidence/source links, and a maturity or risk note. Organize the items into "今日重点" and "值得继续观察" according to step 4. The total number of main and early-signal items should be exactly 10 whenever possible.
   Write in natural, plain Chinese after understanding the source; do not translate the English title, summary, README, or paragraph structure sentence by sentence. Reorganize the facts in your own words, lead with what happened, use concrete actions and limits instead of long abstract noun strings, and explain the first occurrence of unavoidable English terms. Keep claims within the source evidence, label project-reported or independently unverified numbers, and add a concise editorial judgment plus maturity/risk note. Before finishing, reread every item for translation-like syntax and rewrite any sentence that sounds like machine translation.
   The "值得继续观察" section is required, not discretionary: include at least 2 items as specified in step 4, and explain for each what remains uncertain and what concrete future evidence would warrant following up. Before finishing, check that the section exists, its item count meets the minimum (or has an explicit verified-source shortfall), and both sections together contain 10 items whenever possible.
6. Update `outputs/tech-trends.html` as a lightweight HTML redirect/link to today's dated report, then run `python outputs/build_index.py` to refresh `index.html`.
7. Leave the generated `index.html`, `outputs/tech-trends.html`, and dated report in the working tree for the workflow's following commit step. Do not run `git add`, `git commit`, or `git push`; the workflow performs those operations after Codex exits. Do not modify or stage the raw preview, candidates JSON, or any workflow/prompt file.
8. In the final response, report source or publication failures and the path of the generated report.
